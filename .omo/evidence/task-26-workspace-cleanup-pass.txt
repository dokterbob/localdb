Task 26 evidence — CoreML PredictionPipeline extraction

Implementation:
- Read current embed_documents via codegraph_explore before editing.
- Extracted PredictionPipeline in embed/src/pplx_context_coreml.rs.
- embed_documents now composes: empty result slots, plan prediction windows, run pipeline, scatter rows.
- Autorelease pools retained around per-window prediction start and per-completion output extraction.
- Drain-on-error retained: first error stops enqueueing, in-flight predictions drain silently before returning the error.
- Bucket selection remains in prep_window/select_bucket unchanged.

Verification:
- cargo build --workspace: PASS
- cargo test -p embed --features local-coreml: PASS (102 passed, 1 ignored in lib; 2 CoreML device integration tests ignored unless run with --ignored)
- cargo test -p embed: PASS (56 passed)
- git diff --stat cd8fbb3..HEAD -- core/src/ingestion.rs: PASS (no output)
- Pure LOC for embed/src/pplx_context_coreml.rs: 828 (pre-existing oversized CoreML module; refactor was scoped to the in-flight pipeline hotspot)
